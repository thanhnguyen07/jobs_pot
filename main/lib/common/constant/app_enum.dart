enum CreateScreenType {
  post,
  createJob,
}

enum UploadImageType {
  avatar,
  background,
}

enum ProviderType {
  google,
  password,
  facebook,
}

enum ApiLogType {
  error,
  request,
  response,
}
